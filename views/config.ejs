<%- include('partials/header', { active: 'config' }) %>

<%- include('partials/flash') %>

<h1>⚙️ Minha conta</h1>

<div class="card">
  <h3>Alterar nome</h3>
  <p class="texto-aux">Seu nome aparece no ranking e também serve para fazer login.</p>

  <form method="POST" action="/config/nome" class="form-inline" style="display:flex; gap:8px; align-items:end; flex-wrap:wrap">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <div class="form-group" style="margin:0;flex:1;min-width:200px">
      <label for="nome">Nome</label>
      <input type="text" id="nome" name="nome" class="form-control" value="<%= usuario.nome %>" placeholder="Seu nome" required>
    </div>
    <button type="submit" class="btn btn-primary">💾 Salvar</button>
  </form>
</div>

<div class="card">
  <h3>Foto do perfil</h3>
  <div style="display:flex;align-items:center;gap:20px;flex-wrap:wrap">
    <div style="text-align:center">
      <% if (usuario.foto) { %>
        <img src="<%= usuario.foto %>" alt="Foto de perfil" style="width:100px;height:100px;border-radius:50%;object-fit:cover;border:3px solid var(--verde)">
      <% } else { %>
        <div style="width:100px;height:100px;border-radius:50%;background:var(--cinza);display:flex;align-items:center;justify-content:center;font-size:2.5rem;border:3px solid var(--cinza-escuro)">👤</div>
      <% } %>
    </div>
    <div>
      <form method="POST" action="/config/foto" enctype="multipart/form-data" style="display:flex;gap:8px;align-items:end;flex-wrap:wrap">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="form-group" style="margin:0;flex:1;min-width:200px">
          <label for="foto">Selecionar imagem</label>
          <input type="file" id="foto" name="foto" class="form-control" accept="image/jpeg,image/png,image/gif,image/webp" required style="padding:8px">
        </div>
        <button type="submit" class="btn btn-primary">📷 Enviar</button>
      </form>
      <p class="texto-aux" style="margin-top:4px">JPG, PNG, GIF ou WebP • Máximo 2MB</p>
      <% if (usuario.foto) { %>
        <form method="POST" action="/config/foto/remover" style="margin-top:8px">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <button type="submit" class="btn btn-sm btn-secondary">🗑 Remover foto</button>
        </form>
      <% } %>
    </div>
  </div>
</div>

<div class="card">
  <h3>Suas informações</h3>
  <table style="width:100%">
    <tr><td style="padding:6px 0;font-weight:600;width:120px">Nome:</td><td><%= usuario.nome %></td></tr>
    <tr><td style="padding:6px 0;font-weight:600">E-mail:</td><td><%= usuario.email %></td></tr>
  </table>
</div>

<%- include('partials/footer') %>
