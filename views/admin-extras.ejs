<%- include('partials/header', { active: 'admin' }) %>

<%- include('partials/flash') %>

<h1>🏆 Resultados Extras</h1>
<p class="subtitulo">Defina as seleções vencedoras de cada categoria extra. Os pontos serão somados ao ranking automaticamente.</p>

<form method="POST" action="/admin/extras">
  <div class="grid grid-2">
    <% categorias.forEach(function(cat) { %>
      <div class="card">
        <div class="flex-between">
          <h3 style="margin:0"><%= cat.nome %></h3>
          <span class="pontos-badge"><%= cat.pts %> pts</span>
        </div>
        <div class="form-group mt-2" style="margin-bottom:0">
          <select name="<%= cat.id %>" class="form-control">
            <option value="">— Selecione —</option>
            <% selecoes.forEach(function(s) { %>
              <option value="<%= s.id %>" <%= resultados[cat.id] && resultados[cat.id].selecao_id === s.id ? 'selected' : '' %>>
                <%= s.nome_pt %>
              </option>
            <% }); %>
          </select>
          <% if (resultados[cat.id]) { %>
            <p class="texto-aux mt-1" style="color:var(--verde)">✅ Salvo</p>
          <% } %>
        </div>
      </div>
    <% }); %>
  </div>

  <div class="text-center mt-3 mb-2">
    <button type="submit" class="btn btn-primary btn-lg">💾 Salvar resultados</button>
  </div>
</form>

<%- include('partials/footer') %>
